{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "start client",
      "type": "shell",
      "command": "pnpm nx serve client",
      "isBackground": true,
      "problemMatcher": {
        "pattern": {
          "regexp": "^.*$"
        },
        "background": {
          "activeOnStart": true,
          "beginsPattern": ".*",
          "endsPattern": "Local:.*(localhost:\\d+)"
        }
      }
    }
  ]
}
